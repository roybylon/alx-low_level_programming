#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char str2[] = "with proper grammar, but the outcome is a piece of art,";
printf("%s", str2);
return (0);
}
